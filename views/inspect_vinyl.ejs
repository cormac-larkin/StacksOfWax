<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="./globals.css" />
    <link rel="stylesheet" href="./navbar.css" />
    <link rel="stylesheet" href="./footer.css" />
    <link rel="stylesheet" href="./inspect_vinyl.css" />
    <title><%= tracks[0].vinyl %> | <%= tracks[0].artist %></title>
  </head>
  <body>
    <%- include("./partials/navbar") %>

    <div class="flex-container">
      <main class="main">
        <header class="page-header">
          <div class="album-cover">
            <img class="cover-image" src="<%= tracks[0].image_url %>" alt="" />
          </div>
          <div class="track-names">
            <h2 class="header-text"><%= tracks[0].vinyl %> | <%= tracks[0].artist %></h2>
            <h4 class="vinyl-year"><%= tracks[0].genre %> | <%= tracks[0].year %></h4>
            <% for (let i = 0; i < tracks.length; i++) { %>
                <div class="track">
                    <% if (i > 8) { %>
                        <h3.track-name><%= (i+1) %>&nbsp; | &nbsp; <b><%= tracks[i].name %></b></h3.track-name>
                    <% } else { %>
                        <h3.track-name>&nbsp;<%= (i+1) %> &nbsp; | &nbsp; <b><%= tracks[i].name %></b></h3.track-name>
                    <% } %>
                </div>
            <% } %>
          </div>
        </header>
      </main>
    </div>
    <%- include("./partials/footer") %>
  </body>
  <script src="./navbar.js"></script>
</html>
